<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ site.title }}{% if title %} - {{title}}{% endif %}</title>
        <meta name="description" 
              content=
              "{%if description %}{{description}}
                {%else%}{{site.description}}
                {%endif%}">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="{{ '/assets/css/tailwind.css' | url }}">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600&display=swap">
    </head>
    <body>
        {% include "partials/_navigation.liquid" %}
        <main class="my-48 max-w-3xl mx-auto py-12 min-h-screen">
            {{ content }}
        </main>
        {% include "partials/_footer.liquid" %}
    </body>
</html>
